[package]
name = "fence-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
common = { path = "../common" }
fence-model = { path = "../model" }
tokenizer = { path = "../tokenizer" }
